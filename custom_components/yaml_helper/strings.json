{
  "title": "Store options in key / value pairs",
  "config": {
    "step": {
      "user": {
        "title": "Store options in key / value pairs",
        "description": "Store options in key / value pairs.",
        "data": {
          "name": "Name of the sensor",
          "values": "Values"
        },
        "data_description": {
          "values": "Sensor values in yaml format"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "values": "Values"
        },
        "data_description": {
          "values": "Sensor values in yaml format"
        }
      }
    }
  }
}
